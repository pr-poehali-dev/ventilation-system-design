# Pinned Python deps for the desktop build (server.exe packaging).
# Fixed versions => identical build on any PC (Python 3.11 / 3.12).
# Update deliberately, then re-test the build.
pyinstaller==6.11.1
flask==3.1.0
numpy==2.1.3
svglib==1.5.1
reportlab==4.2.5
